public int[] minMax(int[] nums, int begin, int end) {
	int[] num1 = {0,0}
	int min = 1000;
	int max = -1000;
	return (getMinMax(nums, begin, end,num1,min,max));
	
  }
  public int[] getMinMax(int[] nums, int begin, int end, int min, int max){
	  if(begin==end){
		if(max < nums[begin]){
		  max = nums[begin];
		}
	  
		if(min > nums[i]){
		  min = nums[i]
		}
		return;
	  }
	  if(end - begin ==1){
		if (nums[begin]<nums[end})
	  }
  }